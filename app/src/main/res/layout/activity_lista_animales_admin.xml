<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        >
        <ImageView
            android:id="@+id/irHome"
            android:layout_width="100dp"
            android:layout_height="100dp"
            android:src="@drawable/logo"
            android:layout_marginStart="16dp"/>

        <ImageButton
            android:id="@+id/botonCrearAnimal"
            android:layout_width="80dp"
            android:layout_height="80dp"
            android:layout_marginStart="200dp"
            android:layout_marginTop="10dp"
            android:background="@drawable/icono_crear_animal"
            android:contentDescription="@string/a_adir_usuario"
            android:hint="@string/boton_usuarios"
            android:maxWidth="50dp"
            android:maxHeight="50dp"
            android:scaleType="fitCenter" />
    </LinearLayout>

    <ListView
        android:id="@+id/lista_animales_admin"
        android:layout_width="match_parent"
        android:layout_height="match_parent">
    </ListView>

</LinearLayout>